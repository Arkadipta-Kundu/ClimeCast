<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ClimeCast</title>
    <!-- Tailwind CSS -->
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>

<body class="bg-gray-100">
    <div class="container mx-auto mt-10 p-6 shadow-lg rounded-lg bg-white">
        <h1 class="text-center mb-6 text-3xl font-extrabold text-gray-800">City Dashboard</h1>
        <form method="POST" action="/" class="text-center mb-6">
            <div class="flex justify-center mb-4">
                <input type="text" name="cityName" class="form-input w-1/2 p-3 border border-gray-300 rounded-l-lg focus:outline-none focus:ring-2 focus:ring-blue-500" placeholder="Enter city name" required>
                <button type="submit" class="bg-blue-500 text-white p-3 rounded-r-lg hover:bg-blue-600 focus:outline-none focus:ring-2 focus:ring-blue-500">Get Info</button>
            </div>
        </form>

        <div class="weather mb-6">
            <h2 class="text-blue-500 text-2xl font-semibold mb-4">Weather</h2>
            <% if (weather) { %>
                <div class="p-4 bg-blue-50 rounded-lg shadow-md">
                    <p class="text-lg">Temperature: <%= weather.temp %>°C</p>
                    <p class="text-lg">Condition: <%= weather.condition %></p>
                </div>
            <% } else if (weather === null) { %>
                <div class="alert alert-danger text-red-500">No weather data found. Please try again.</div>
            <% } %>
        </div>

        <div class="news">
            <h2 class="text-blue-500 text-2xl font-semibold mb-4">News</h2>
            <% if (news) { %>
                <% news.forEach(article => { %>
                    <div class="card mb-4 border border-gray-300 rounded-lg shadow-md">
                        <div class="card-body p-6">
                            <h5 class="card-title text-xl font-bold text-gray-800 mb-2"><%= article.title %></h5>
                            <p class="card-text text-gray-700"><%= article.description %></p>
                        </div>
                    </div>
                <% }) %>
            <% } else if (news === null) { %>
                <div class="alert alert-danger text-red-500">No news found. Please try again.</div>
            <% } %>
        </div>
    </div>
</body>

</html>
